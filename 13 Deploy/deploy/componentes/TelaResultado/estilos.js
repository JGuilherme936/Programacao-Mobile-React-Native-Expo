import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#fff",
    padding: 20,
  },
  img: { width: 200, height: 200, marginBottom: 30 },
  textoResultado: { fontSize: 32, fontWeight: "bold", marginBottom: 40 },
});
