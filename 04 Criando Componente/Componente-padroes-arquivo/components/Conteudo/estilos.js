import { StyleSheet } from 'react-native';

const estilos = StyleSheet.create({
    container: {
        padding: 20,
        alignItems: 'center',
    },
    titulo: {
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 20,
        textAlign: 'center'
    },
    img: {
        width: '100%',
        height: 200,
        borderRadius: 10,
        marginBottom: 20,
    },
    paragrafo: {
        fontSize: 16,
        lineHeight: 24,
        textAlign: 'center',
        color: '#444'
    }
});

export default estilos;
