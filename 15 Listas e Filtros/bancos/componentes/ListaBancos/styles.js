import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  Container: { padding: 10, flex: 1 },
  CampoBusca: { backgroundColor: 'white', height: 40, marginBottom: 12, paddingHorizontal: 10, borderWidth: 1, borderColor: '#ccc', borderRadius: 5 }
});
export default styles;
